<% provide(:title, '| Home') %>
<% if signed_in? %>
  <%= render 'shared/signin_user' %> 
  <section>
    <%= render 'shared/user_info' %>
  </section>
  <section>
    <%= render 'shared/stats' %>
  </section>
  <section>
    <%= render 'shared/micropost_form' %>
  </section>
<% else %>
  <%= render 'shared/not_a_user' %>
<% end %> 
